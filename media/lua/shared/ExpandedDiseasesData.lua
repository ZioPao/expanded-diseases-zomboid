ExpandedDiseases = {
	-- Maybe we can use this to make a settings menu at some point if we want by iterating all keys
    Settings = { 
		Diabetes = {
			TicksToProgress = 20,
		}
	},
    Client = {
    },
    Server = {
    },
}